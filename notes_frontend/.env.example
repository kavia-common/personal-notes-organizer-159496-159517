# Frontend environment variables (consumed at runtime via public/assets/env.js)
# Copy these into public/assets/env.js or set via your deployment platform.
# The app reads window.__env.NOTES_API_BASE_URL at runtime; leave empty to use localStorage mode.

NOTES_API_BASE_URL=
