import { Routes } from '@angular/router';

/**
 * This app uses a single-page layout with internal state, no additional routes.
 * Keep this for future extensibility.
 */
export const routes: Routes = [];
